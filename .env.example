# API-Football / API-Sports direct free plan key (100 requests/day).
# Register at https://www.api-football.com/ and copy your key here.
# Local MVP only: this key is exposed to the browser frontend. Before any
# public deployment it MUST move behind a server-side/serverless proxy.
VITE_API_FOOTBALL_KEY=
